package fi.darklake.wallet.ui.theme

import androidx.compose.ui.graphics.Color

// Darklake Primary Brand Colors
val NeonGreen = Color(0xFF00ff41)         // Primary brand neon green
val MutedGreen = Color(0xFF00aa00)        // Muted dark green
val TerminalGreen = Color(0xFF33ff33)     // Brighter terminal green
val GreenGlow = Color(0x4000ff41)         // Semi-transparent green for glow effects

// Background Colors
val NearBlack = Color(0xFF0a0a0a)         // Primary background
val DarkGray = Color(0xFF1a1a1a)          // Secondary background
val TerminalBlack = Color(0xFF000000)     // Pure black for terminal
val ScanlineOverlay = Color(0x1100ff41)   // Very subtle green for scanlines

// Darklake Secondary Colors  
val ElectricBlue = Color(0xFF0080ff)      // Electric blue accent
val BrightCyan = Color(0xFF00ffff)        // Bright cyan/teal
val DeepNavy = Color(0xFF001a33)          // Deep navy for subtle accents
val MatrixBlue = Color(0xFF003366)        // Matrix-style blue

// Terminal/Text Colors
val TerminalWhite = Color(0xFFc0c0c0)     // Muted white for text
val TerminalGray = Color(0xFF808080)      // Gray for secondary text
val ErrorRed = Color(0xFFff3333)          // Error states
val WarningAmber = Color(0xFFffaa00)      // Warning states

// UI State Colors
val SurfaceVariant = Color(0xFF2a2a2a)    // Card/surface backgrounds
val SurfaceContainer = Color(0xFF1f1f1f)  // Container backgrounds
val OnSurface = Color(0xFFc0c0c0)         // Text on surfaces
val OnSurfaceVariant = Color(0xFF808080)  // Secondary text
val Outline = Color(0xFF404040)           // Borders and dividers
val OutlineVariant = Color(0xFF333333)    // Subtle borders

// Interactive Colors
val ButtonPrimary = Color(0x0000ff41)     // Transparent green for buttons
val ButtonHover = Color(0x3300ff41)       // Green hover state
val ButtonPressed = Color(0x6600ff41)     // Green pressed state
val RippleEffect = Color(0x3300ff41)      // Ripple color

// Special Effects Colors
val GlowEffect = Color(0x8000ff41)        // Glow around elements
val ShadowColor = Color(0x80000000)       // Drop shadows
val GridLines = Color(0x1a00ff41)         // Grid pattern overlay
val NoiseOverlay = Color(0x0a808080)      // Noise texture